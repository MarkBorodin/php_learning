{% extends 'main/base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="row no-gutters">
            <div class="col-12">
                <h1 class="display-4 text-center mt-5">
                    {{ title }}
                </h1>
            </div>
        </div>
    </div>


    <div class="container">
        <div class="row no-gutters justify-content-center align-items-center mt-5">
            <div class="col-6">
                {{ form(form) }} <button class="btn btn-secondary btn-lg btn-block" onClick="window.history.back();">back</button>
            </div>
        </div>
    </div>

{% endblock %}